function love.conf(t)
    t.window.width = 500         -- Initial window width
    t.window.height = 500        -- Initial window height
    t.window.title = "PODO"
    -- t.window.minwidth = 400      -- Minimum window width
    -- t.window.minheight = 400     -- Minimum window height
    -- t.window.resizable = true    -- Allows the window to be resized by the user
end
